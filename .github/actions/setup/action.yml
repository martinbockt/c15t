name: "Setup"
description: "Setup Bun, install dependencies, and optionally install Playwright"

inputs:
  playwright:
    description: "Install Playwright browsers (chromium)"
    required: false
    default: "false"

runs:
  using: "composite"
  steps:
    - name: Setup Bun
      uses: oven-sh/setup-bun@f4d14e03ff726c06358e5557344e1da148b56cf7
      with:
        bun-version: "1.3.11"

    - name: Cache bun dependencies
      uses: actions/cache/restore@0057852bfaa89a56745cba8c7296529d2fc39830
      with:
        path: ~/.bun/install/cache
        key: ${{ runner.os }}-bun-${{ hashFiles('bun.lock') }}
        restore-keys: ${{ runner.os }}-bun-

    - name: Install dependencies
      shell: bash
      run: bun install --frozen-lockfile

    - name: Cache Playwright browsers
      if: inputs.playwright == 'true'
      uses: actions/cache/restore@0057852bfaa89a56745cba8c7296529d2fc39830
      with:
        path: ~/.cache/ms-playwright
        key: ${{ runner.os }}-playwright-${{ hashFiles('package.json') }}
        restore-keys: ${{ runner.os }}-playwright-

    - name: Install Playwright
      if: inputs.playwright == 'true'
      shell: bash
      run: bunx playwright@1.58.2 install --with-deps chromium chromium-headless-shell
