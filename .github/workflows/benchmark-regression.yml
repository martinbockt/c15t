name: Benchmark Regression

on:
  # TODO: re-enable the pull_request trigger once the report is less noisy.
  # Disabled for now — the per-PR comparison comment is too spammy.
  # pull_request:
  #   types: [opened, synchronize, reopened]
  #   paths:
  #     - 'packages/core/**'
  #     - 'packages/react/**'
  #     - 'packages/nextjs/**'
  #     - 'packages/translations/**'
  #     - 'packages/ui/**'
  #     - 'benchmarks/**'
  #     - 'package.json'
  #     - 'bun.lock'
  #     - 'bun.lockb'
  #     - 'turbo.json'
  #     - 'internals/bundle-analysis-action/**'
  workflow_dispatch:
  push:
    branches: ['main', 'canary']
    paths:
      - 'packages/core/**'
      - 'packages/react/**'
      - 'packages/nextjs/**'
      - 'packages/translations/**'
      - 'packages/ui/**'
      - 'benchmarks/**'
      - 'package.json'
      - 'bun.lock'
      - 'bun.lockb'
      - 'turbo.json'
      - 'internals/bundle-analysis-action/**'

env:
  # leadtype package-docs generation exceeds the default Node heap on
  # cache-miss builds of larger packages (core, react).
  NODE_OPTIONS: --max-old-space-size=6144

jobs:
  benchmark:
    name: Run Benchmarks
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Check out base repository
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          ref: ${{ github.event.pull_request.base.ref || github.ref }}
          fetch-depth: 0
          persist-credentials: false

      - name: Setup Bun
        uses: oven-sh/setup-bun@f4d14e03ff726c06358e5557344e1da148b56cf7
        with:
          bun-version: "1.3.11"

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Install Playwright Chromium
        run: |
          PLAYWRIGHT_VERSION=$(bun -e "console.log(require('./package.json').devDependencies.playwright)")
          bunx "playwright@${PLAYWRIGHT_VERSION}" install --with-deps chromium chromium-headless-shell

      - name: Clean benchmark output
        run: rm -rf .benchmarks .perf

      - name: Run base benchmark suite
        id: run-base-bench
        continue-on-error: ${{ github.event_name == 'pull_request' }}
        env:
          TURBO_TOKEN: ${{ github.event_name != 'pull_request' && secrets.TURBO_TOKEN || '' }}
          TURBO_TEAM: ${{ github.event_name != 'pull_request' && secrets.TURBO_TEAM || '' }}
        run: bun run bench:ci

      - name: Save base results
        if: github.event_name == 'pull_request' && steps.run-base-bench.outcome == 'success'
        run: |
          mkdir -p .perf
          mv .benchmarks/head .perf/base

      - name: Check out PR branch
        if: github.event_name == 'pull_request'
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          clean: false
          persist-credentials: false

      - name: Reinstall dependencies for PR branch
        if: github.event_name == 'pull_request'
        run: bun install --frozen-lockfile

      - name: Reinstall Playwright Chromium for PR branch
        if: github.event_name == 'pull_request'
        run: |
          PLAYWRIGHT_VERSION=$(bun -e "console.log(require('./package.json').devDependencies.playwright)")
          bunx "playwright@${PLAYWRIGHT_VERSION}" install --with-deps chromium chromium-headless-shell

      - name: Run head benchmark suite
        if: github.event_name == 'pull_request'
        id: run-head-bench
        env:
          TURBO_TOKEN: ''
          TURBO_TEAM: ''
        run: bun run bench:ci

      - name: Save head results
        if: github.event_name == 'pull_request' && steps.run-head-bench.outcome == 'success'
        run: |
          mkdir -p .perf
          mv .benchmarks/head .perf/head

      - name: Compare benchmark reports
        if: github.event_name == 'pull_request' && steps.run-base-bench.outcome == 'success' && steps.run-head-bench.outcome == 'success'
        env:
          BENCHMARK_BASE_DIR: .perf/base
          BENCHMARK_HEAD_DIR: .perf/head
          BENCHMARK_COMPARE_DIR: .perf/compare
          BENCHMARK_ENFORCE: ${{ vars.C15T_BENCHMARK_ENFORCE || 'false' }}
        run: bun run bench:compare

      - name: Note skipped base comparison
        if: github.event_name == 'pull_request' && steps.run-base-bench.outcome != 'success'
        run: |
          mkdir -p .perf/compare
          cat <<'EOF' > .perf/compare/summary.md
          Base benchmark suite failed on the target branch, so regression comparison was skipped for this PR run.
          Head benchmark results were still collected from the PR branch.
          EOF

      - name: Save PR metadata
        if: github.event_name == 'pull_request'
        env:
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          mkdir -p .perf/metadata
          echo "$PR_NUMBER" > .perf/metadata/pr-number

      - name: Debug perf directory contents
        if: github.event_name == 'pull_request'
        run: ls -la .perf/ && find .perf -type f | head -20 || echo "No .perf directory found"

      - name: Upload regression artifacts
        if: github.event_name == 'pull_request'
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: benchmark-regression-result
          path: |
            .perf/base/
            .perf/head/
            .perf/compare/
            .perf/metadata/
          if-no-files-found: warn
          retention-days: 3

      - name: Upload push artifacts
        if: github.event_name != 'pull_request'
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: benchmark-current
          path: .benchmarks/head/
          if-no-files-found: error
          retention-days: 3
